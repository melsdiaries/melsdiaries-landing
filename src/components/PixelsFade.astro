---
import { MELSDIARIES_APP_URL } from "../consts";
---

<section class="py-24 max-w-7xl mx-auto px-6">
  <div class="text-center mb-16">
    <h2 class="text-4xl md:text-5xl font-serif leading-tight mb-6">
      Pixels Fade. Feelings Don't Have To.
    </h2>
    <p class="text-brand-dark/70 max-w-2xl mx-auto leading-relaxed">
      Digital photos vanish with broken phones and forgotten passwords. A
      physical photobook stays forever, becoming more precious with time.
    </p>
  </div>

  <div class="space-y-2 mb-12">
    <!-- Top row: 60-40 width ratio -->
    <div class="grid grid-cols-2 gap-2">
      <div class="bg-brand-beige rounded-2xl overflow-hidden">
        <img
          src="/pixels-fade/11.png"
          alt="Memory moment"
          class="w-full h-[300px] object-cover"
        />
      </div>
      <div class="bg-brand-beige rounded-2xl overflow-hidden">
        <img
          src="/pixels-fade/12.png"
          alt="Cherished memory"
          class="w-full h-[300px] object-cover"
        />
      </div>
    </div>

    <!-- Bottom row: 30-70 width ratio -->
    <div class="grid grid-cols-10 gap-2">
      <div class="col-span-3 bg-brand-beige rounded-2xl overflow-hidden">
        <img
          src="/testimonial/1.png"
          alt="Precious moment"
          class="w-full h-[300px] object-cover"
        />
      </div>
      <div class="col-span-7 bg-brand-beige rounded-2xl overflow-hidden">
        <img
          src="/pixels-fade/13.png"
          alt="Special memory"
          class="w-full h-[300px] object-cover"
        />
      </div>
    </div>
  </div>

  <div class="text-center">
    <a
      href={MELSDIARIES_APP_URL}
      class="inline-block bg-brand-orange text-white px-8 py-3 rounded-md font-bold transition-transform hover:scale-105"
    >
      Create your book
    </a>
  </div>
</section>
